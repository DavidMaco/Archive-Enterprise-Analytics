# Streamlit Cloud deployment dependencies.
# Streamlit Cloud installs: `pip install -r requirements.txt` then `pip install .` (from pyproject.toml).
# Local pinned install: `pip install -c constraints.txt -r requirements.txt`

pandas==2.3.3
numpy==2.4.2
pyarrow==23.0.1
scipy==1.17.0
streamlit==1.54.0
plotly==6.5.2
scikit-learn==1.8.0
joblib==1.5.3
